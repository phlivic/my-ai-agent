# WebSocket endpoint for the backend agent.
# For production builds, set this to your deployed Worker URL + agent path.
# Example:
# VITE_AGENT_WS_URL=wss://edge-ai-backend.<your-subdomain>.workers.dev/agents/my-agent/default
VITE_AGENT_WS_URL=
